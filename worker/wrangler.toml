# Seattle Parks activity-search GET shim.
# Deployed to *.workers.dev (no custom domain, not wired into the OpenTofu infra).
# See ./README.md and coryking/home-it-services#77 for the credential dependency.

name = "seattle-activities"
main = "src/index.js"
compatibility_date = "2026-06-23"

# Not secret (already in infrastructure/variables.tf). wrangler also honors
# CLOUDFLARE_ACCOUNT_ID from the env if you'd rather not hardcode it.
account_id = "8984406411c5606d3f2d4feb5ca316ae"

# Publish the *.workers.dev route.
workers_dev = true
