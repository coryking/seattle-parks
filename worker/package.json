{
  "name": "seattle-activities",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP server (Streamable HTTP) over the Seattle Parks & Rec ActiveCommunities catalog.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "^0.16.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
